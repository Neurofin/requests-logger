apiVersion: v1
kind: Service
metadata:
    name: classifier
spec:
    ports:
        - port: 80
          targetPort: 3006
          protocol: TCP
    type: ClusterIP
    selector:
        app: classifier
