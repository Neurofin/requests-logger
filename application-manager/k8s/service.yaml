apiVersion: v1
kind: Service
metadata:
    name: application-manager
spec:
    ports:
        - port: 80
          targetPort: 3003
          protocol: TCP
    type: ClusterIP
    selector:
        app: application-manager
